import type { LucidShared } from '@trgames/shared';

// Временная заглушка: настоящая реализация появится в задаче 8
export const resolveOption = (
  G: LucidShared.TG,
  actorId: LucidShared.TPlayerId,
  option: LucidShared.TOption,
): LucidShared.TG | null => {
  void actorId;
  void option;

  return G;
};
