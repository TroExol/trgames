# Инструкции для агентов

- Отвечайте на русском языке.
- При изменении общих правил обновляйте этот файл, чтобы информация оставалась актуальной.
- При необходимости запускайте тесты серверной части через `yarn --cwd server vitest run <path>` или полный набор скриптом `yarn --cwd server test`.
